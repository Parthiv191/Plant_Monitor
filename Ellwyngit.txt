When you open
     - git pull
     - cd PlantMonitor
     - code . (this will open new vs code window)
     - in the new window (cmd + shift + p) or type > in the bar at the top
     - type "reopen in container" and click on docker rebuild or reopen in container

When you make a change 

Check status 
      git status

1.    git add .
2.    git commit -m "Your message for commiting"
3.    git push -u 